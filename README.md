# คู่มือการติดตั้งและใช้งาน N-Body Simulation

ขั้นตอนการติดตั้งเครื่องมือ การดาวน์โหลดซอร์สโค้ด และการรันโปรแกรม N-Body Simulation ด้วย CUDA

## 1. การติดตั้งเครื่องมือ (Installation)

ก่อนเริ่มใช้งาน ตรวจสอบให้แน่ใจว่าได้ติดตั้งโปรแกรมพื้นฐานต่อไปนี้:

1.  **Visual Studio 2022** (เลือก Workload: *Desktop development with C++*)
2.  **NVIDIA CUDA Toolkit**

### ตรวจสอบความถูกต้อง (Verification)

หลังจากติดตั้งเสร็จ ให้ตรวจสอบว่าระบบมองเห็น CUDA Compiler หรือไม่:

1.  เปิด **cmd** (Command Prompt)
2.  พิมพ์คำสั่ง:
    ```cmd
    nvcc --version
    ```
    *(หากติดตั้งถูกต้อง จะต้องขึ้นเวอร์ชันของ Cuda compilation tools)*

-----

## 2. การดาวน์โหลดโปรเจกต์ (Clone Project)

1.  เปิด **Git Bash** หรือ Terminal ที่คุณถนัด
2.  พิมพ์คำสั่งเพื่อดาวน์โหลดโค้ด:
    ```bash
    git clone https://github.com/Chonapatcc/N-Body-Simulation-with-CUDA.git
    ```

-----

## 3. การคอมไพล์และรันโปรแกรม (Compile & Run)

**สำคัญ:** ขั้นตอนนี้ต้องทำผ่าน **x64 Native Tools Command Prompt for VS 2022** เท่านั้น เพื่อให้สามารถเรียกใช้ `g++` และ `nvcc` ได้อย่างถูกต้อง

1.  เปิดโปรแกรม **x64 Native Tools Command Prompt for VS 2022**
2.  เข้าไปยังโฟลเดอร์ `src` ของโปรเจกต์:
    ```cmd
    cd N-Body-Simulation-with-CUDA\src
    ```

### 🔹 แบบที่ 1: Sequential Bruteforce

การคำนวณแบบพื้นฐาน (ช้าที่สุด แต่แม่นยำที่สุด)

  * **Compile:**
    ```cmd
    g++ sequential_bruteforce.cc -o sequential_bruteforce.exe
    ```
  * **Run:**
    ```cmd
    sequential_bruteforce.exe
    ```

### 🔹 แบบที่ 2: Sequential Barnes-Hut (Best CPU)

การคำนวณโดยใช้อัลกอริทึม Barnes-Hut บน CPU

  * **Compile:**
    ```cmd
    g++ sequential_best.cc -o sequential_best.exe
    ```
  * **Run:**
    ```cmd
    sequential_best.exe
    ```

### 🔹 แบบที่ 3: CUDA Barnes-Hut (GPU Accelerated)

การคำนวณโดยใช้การ์ดจอ (เร็วที่สุด)

  * **Compile:**
    ```cmd
    nvcc cuda_best.cu -o cuda_best.exe
    ```
  * **Run:**
    ```cmd
    cuda_best.exe
    ```

-----

## 4. การดูผลลัพธ์ (Results)

เมื่อโปรแกรมทำงานเสร็จสิ้น สามารถเข้าไปดูผลลัพธ์ได้ที่โฟลเดอร์ **`result`** ซึ่งจะประกอบไปด้วย 2 ไฟล์ ต่อรอบการรัน (แยกตามจำนวน N):

  * **`...force_N.csv`**: ค่าแรงลัพธ์ที่คำนวณได้ (Final Force)
  * **`...time_N.txt`**: เวลาที่ใช้ในการประมวลผล (Time Usage)